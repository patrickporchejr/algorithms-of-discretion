% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grounding_experiment.R
\name{run_grounding_experiment}
\alias{run_grounding_experiment}
\title{Run the naive-vs-grounded LLM datasheet-grounding experiment}
\usage{
run_grounding_experiment(
  data_path,
  datasheet_path,
  providers,
  models,
  questions = grounding_questions(),
  id_cols = "county_fips",
  n_repeats = 1
)
}
\arguments{
\item{data_path}{Path to the audit-ready CSV (e.g.
\code{data/processed/audit_ready_stops.csv}).}

\item{datasheet_path}{Path to \code{datasheet.json}.}

\item{providers}{Character vector, any of \code{"anthropic"}, \code{"openai"},
\code{"gemini"}, \code{"grok"}.}

\item{models}{Named list mapping each requested provider to a model
string, e.g. \code{list(anthropic = "claude-opus-5", openai = "gpt-5.1", gemini = "gemini-3.1-pro-preview", grok = "grok-4.6")}.}

\item{questions}{Question battery. Default \code{\link[=grounding_questions]{grounding_questions()}}.}

\item{id_cols}{Columns to pseudonymize in the naive/grounded data context
before either condition sees it -- see \code{\link[=build_data_context]{build_data_context()}}. Default
\code{"county_fips"}, this project's real-world geographic identifier.}

\item{n_repeats}{Independent trials per (provider, condition), same
prompt each time. A single trial's "changed answer" could just be
sampling noise (providers aren't run at temperature 0 -- see
\code{llm_clients.R} for why); repeats let \code{\link[=summarize_grounding_trials]{summarize_grounding_trials()}}
report a majority-vote answer and a stability/agreement rate instead
of trusting one draw. Default 1.}
}
\value{
An object of class \code{duboisR_grounding_result} wrapping a tibble
\code{results} with one row per (provider, condition, trial, question):
\code{provider}, \code{model}, \code{condition}, \code{trial}, \code{question_id}, \code{type},
\code{prompt}, \code{answer}, \code{confidence}, \code{expected_answer}, \code{correct},
\code{rationale}. Pass \code{results} to \code{\link[=summarize_grounding_trials]{summarize_grounding_trials()}} to
collapse repeats into one row per question.
}
\description{
For every requested provider, calls the same flagship model twice --
once "naive" (dataset description only) and once "grounded" (dataset
description plus \code{datasheet.json}, with an explicit instruction to
consult it) -- over the fixed \code{\link[=grounding_questions]{grounding_questions()}} battery, and scores
each answer against that question's hand-authored \code{expected_answer}. The
Datasheets-for-Datasets literature's central claim -- that provenance
documentation changes how a dataset gets used -- made empirically
checkable for one concrete downstream consumer (an LLM answering
questions about the data) instead of asserted.
}
