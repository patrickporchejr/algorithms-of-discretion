% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/proxy_diagnostics.R
\name{check_proxies}
\alias{check_proxies}
\title{Check whether "neutral" covariates act as proxies for a protected attribute}
\usage{
check_proxies(
  data,
  protected_attr = "subject_race",
  predictors,
  method = c("rf", "glm"),
  test_prop = 0.2,
  seed = NULL,
  warn_threshold = 0.1
)
}
\arguments{
\item{data}{A data frame.}

\item{protected_attr}{String column name of the protected attribute
(e.g. \code{"subject_race"}). Default \code{"subject_race"}.}

\item{predictors}{Character vector of column names to test as candidate
proxies. Must not include \code{protected_attr} itself.}

\item{method}{\code{"rf"} (default) uses \code{\link[ranger:ranger]{ranger::ranger()}} if installed;
\code{"glm"} is a dependency-free one-vs-rest \code{\link[stats:glm]{stats::glm()}} fallback.}

\item{test_prop}{Proportion of rows (stratified by \code{protected_attr}) held
out for evaluation. Default \code{0.2}.}

\item{seed}{Optional RNG seed for the train/test split.}

\item{warn_threshold}{\code{lift} above which the result is \code{flagged}. Default
\code{0.10}.}
}
\value{
A list of class \code{duboisR_proxy_check} with \code{accuracy},
\code{baseline_accuracy} (no-information rate), \code{lift}, \code{confusion_matrix},
\code{importance} (named numeric vector, which columns are acting as
proxies), \code{flagged}, and \code{message}.
}
\description{
The Wells-Du Bois Identity Proxy test: temporarily treats a protected
attribute (e.g. \code{subject_race}) as the dependent variable and trains a
predictive model on the remaining covariates. "Excluding" a protected
attribute from a model does not remove race information if it is encoded
in other columns (e.g. zip code, county, vehicle age) — if this function
can predict the protected attribute well above baseline from covariates
the researcher intended to be "neutral," those covariates are acting as
identity proxies. This merges what were described as two identical
proposals ("Identity Proxy Check" and \code{check_proxies()}) across the two
source documents into one function.
}
