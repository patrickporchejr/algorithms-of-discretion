% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grounding_experiment.R
\name{build_data_context}
\alias{build_data_context}
\title{Describe a dataset compactly enough to hand to an LLM}
\usage{
build_data_context(
  data,
  n_sample = 20,
  seed = 20240101,
  id_cols = character(0)
)
}
\arguments{
\item{data}{A data frame.}

\item{n_sample}{Number of sample rows to include. Default 20.}

\item{seed}{Seed for the row sample, for a reproducible prompt across
repeated runs. Default \code{20240101} (same convention as
\code{precompute_audit.R}'s identity-proxy sample).}

\item{id_cols}{Character vector of column names to pseudonymize as opaque
\code{"region_NN"} labels (real-world geographic/administrative identifiers
like county FIPS codes) before sampling. Default \code{character(0)}.}
}
\value{
A single character string.
}
\description{
Stands in for "the dataset" in the naive/grounded prompts: a full
multi-million-row CSV doesn't fit in a prompt, so this instead reports
column names/types/row count plus a small fixed-seed random sample of
rows. Used identically by both conditions in \code{\link[=run_grounding_experiment]{run_grounding_experiment()}}
— only the "grounded" condition additionally receives \code{datasheet.json}
content, so any difference in answers is attributable to that, not to a
different view of the raw data.
}
\details{
\code{id_cols} and every \code{Date}/\code{POSIXct} column are pseudonymized/relativized
in the sample before it's shown (e.g. a real \code{county_fips} like \code{48171}
becomes \code{"region_07"}; a real date becomes \code{"day_412"}, an offset from
the data's own earliest date). Without this, a raw sample of real-world
identifiers hands the "naive" condition a shortcut that has nothing to do
with the datasheet: e.g. every Texas FIPS code starts with \code{48}, which a
knowledgeable model can recognize on sight, and a real calendar date
range reveals this project's year restriction directly. The columns'
\emph{types} are still reported honestly (e.g. \code{date (Date)}) — only the
values are obscured, so this doesn't misrepresent the schema, just the
specific real-world provenance a sample of raw values would otherwise
leak for free.
}
