% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grounding_experiment.R
\name{extract_answer_field}
\alias{extract_answer_field}
\title{Safely pull one field out of one question's \verb{\{answer, confidence\}} entry}
\usage{
extract_answer_field(entry, field)
}
\arguments{
\item{entry}{The value at \code{answers[[question_id]]} -- expected to be
\code{list(answer = ..., confidence = ...)} but not guaranteed to be.}

\item{field}{\code{"answer"} or \code{"confidence"}.}
}
\value{
The field's value, or \code{NULL} if \code{entry} isn't a list or has no
such field.
}
\description{
"Forced" structured output isn't "guaranteed": a provider can still
return a bare scalar (e.g. \code{"TRUE"}) for one question instead of the
nested \verb{\{answer, confidence\}} object the schema asked for, and \code{[[}/\code{$}
on a scalar (an atomic vector) errors rather than returning NULL the way
it would on a missing list element. \code{is.list()} guards against that (same
shape as \code{\link[=api_error_body]{api_error_body()}}), treating a non-conforming field as an
outright-missing one -- NA downstream, not an abort of the whole
multi-provider run.
}
\keyword{internal}
