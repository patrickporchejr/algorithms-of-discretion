% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/threshold_test.R
\name{fit_threshold_test}
\alias{fit_threshold_test}
\title{Fit a fast (non-hierarchical, non-MCMC) Threshold Test approximation}
\usage{
fit_threshold_test(
  suff_stats,
  min_searches = 5,
  weight = c("searches", "n", "none"),
  optim_control = list(maxit = 500)
)
}
\arguments{
\item{suff_stats}{Output of \code{\link[=aggregate_sufficient_statistics]{aggregate_sufficient_statistics()}}.}

\item{min_searches}{Minimum \code{S} for a cell to be included in the \verb{(a_r, b_r)} fit (hit rate is undefined/unstable below this). Default \code{5}.
At least 2 qualifying cells per race are required to identify \verb{(a_r, b_r)}.}

\item{weight}{One of \code{"searches"} (default, weight by \code{S} so noisy
small-sample hit rates don't dominate), \code{"n"}, or \code{"none"}.}

\item{optim_control}{Passed as \code{control} to \code{\link[stats:optim]{stats::optim()}}.}
}
\value{
A list of class \code{duboisR_threshold_fit} with \code{race_params}
(tibble: \code{race}, \code{a}, \code{b}, \code{convergence_code}, \code{n_cells_used_in_fit}),
\code{thresholds} (tibble: per-group threshold/predicted hit rate, every
cell including those below \code{min_searches}, flagged \code{low_confidence}),
and \code{summary} (tibble: \code{race}, \code{mean_threshold_weighted},
\code{n_counties}).
}
\description{
Approximates the hierarchical Bayesian Threshold Test of Simoiu,
Corbett-Davies & Goel (2017) for infra-marginality without MCMC. Each
stopped driver of race \code{r} has a latent risk \code{p ~ Beta(a_r, b_r)}
(shared per race across groups — the simplification vs. the full
hierarchical model), and is searched iff \code{p} exceeds a group-specific
threshold \verb{t_\{r,d\}}. Given \verb{(a_r, b_r)}, a group's threshold is \emph{derived
in closed form} from its own observed search rate
(\code{t_rd = qbeta(1 - search_rate, a_r, b_r)}) rather than estimated as a
free per-cell parameter — this is what makes the method fast. \verb{(a_r, b_r)} are then fit once per race via \code{\link[stats:optim]{stats::optim()}}, minimizing
weighted squared error between the model's predicted hit rate and each
group's observed hit rate.
}
\details{
This is a from-first-principles frequentist point-estimate procedure
consistent with the same Beta signal/threshold model as the cited
literature — it is \strong{not} a verbatim reproduction of any published
"fast approximation" implementation. It provides no partial pooling
across sparse groups and no credible intervals (point estimates only).
Describe results as "in the spirit of," not "identical to," Simoiu et
al. 2017 / Pierson et al. 2020.
}
