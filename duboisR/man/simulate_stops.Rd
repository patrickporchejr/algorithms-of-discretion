% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_stops.R
\name{simulate_stops}
\alias{simulate_stops}
\title{Simulate schema-correct synthetic traffic-stop data}
\usage{
simulate_stops(n = 2000, seed = 42, counties = NULL)
}
\arguments{
\item{n}{Number of rows to simulate. Default \code{2000}.}

\item{seed}{RNG seed, for reproducibility. Default \code{42}.}

\item{counties}{Optional character vector of \code{county_fips} codes to
sample from. Defaults to a fixed set of 10 real TX FIPS codes.}
}
\value{
A tibble with columns \code{subject_race}, \code{subject_sex},
\code{search_conducted}, \code{contraband_found}, \code{hour}, \code{date}, \code{violation},
\code{search_basis}, \code{poverty_rate}, \code{median_income}, \code{county_fips}.
}
\description{
Generates synthetic data matching the real \code{audit_ready_stops.csv} data
contract exactly (see DESIGN.md \eqn{\S}4) — used both as the package's
own test fixtures and as the dashboard's dev-data script
(\code{r_dashboard/dev/generate_synthetic_data.R}). Unlike the pipeline's
original synthetic generator, this deliberately does NOT fabricate
\code{subject_age} or \code{is_arrested}, neither of which exist in the real Texas
State Patrol data.
}
\details{
A synthetic race effect is baked into \code{search_conducted} purely so plots
and diagnostics have a visible, non-null signal to check against — it is
not a finding and should never be treated as one.
}
