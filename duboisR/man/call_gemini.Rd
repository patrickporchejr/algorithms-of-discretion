% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm_clients.R
\name{call_gemini}
\alias{call_gemini}
\title{Call Google's Gemini API for a forced structured-output answer}
\usage{
call_gemini(
  system,
  user,
  schema,
  model,
  api_key = require_api_key("GEMINI_API_KEY")
)
}
\arguments{
\item{system}{System prompt string.}

\item{user}{User prompt string (the data context, question battery, and
-- for the "grounded" condition -- the datasheet content).}

\item{schema}{A JSON-Schema list from \code{\link[=grounding_response_schema]{grounding_response_schema()}}.}

\item{model}{Model name, e.g. \code{"claude-opus-5"}.}

\item{api_key}{API key. Default reads \code{GEMINI_API_KEY} from the
environment.}
}
\value{
A named list of \code{question_id -> answer} matching \code{schema}.
}
\description{
Same forced-structured-output contract as \code{\link[=call_anthropic]{call_anthropic()}}/
\code{\link[=call_openai]{call_openai()}}, via Gemini's native JSON response mode
(\code{responseMimeType = "application/json"} + \code{responseSchema}) rather than
a tool/function call -- Gemini returns the schema-conformant JSON
directly as the response text.
}
