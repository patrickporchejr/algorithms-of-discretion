% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm_clients.R
\name{call_openai_compatible}
\alias{call_openai_compatible}
\title{Call an OpenAI-compatible Chat Completions API for a forced-tool answer}
\usage{
call_openai_compatible(
  base_url,
  provider_name,
  system,
  user,
  schema,
  model,
  api_key
)
}
\arguments{
\item{base_url}{The provider's chat completions endpoint.}

\item{provider_name}{Used only in the "no tool call" error message.}

\item{system}{System prompt string.}

\item{user}{User prompt string (the data context, question battery, and
-- for the "grounded" condition -- the datasheet content).}

\item{schema}{A JSON-Schema list from \code{\link[=grounding_response_schema]{grounding_response_schema()}}.}

\item{model}{Model name, e.g. \code{"claude-opus-5"}.}

\item{api_key}{API key. Default reads \code{ANTHROPIC_API_KEY} from the
environment.}
}
\description{
Shared implementation behind \code{\link[=call_openai]{call_openai()}} and \code{\link[=call_grok]{call_grok()}} -- xAI's
Grok API is explicitly OpenAI-compatible (same \verb{/chat/completions} shape,
same Bearer auth, same \code{tools}/\code{tool_choice} contract), so this is one
request-building path parameterized by base URL and provider name (used
only for the error message) rather than two near-identical copies.
}
\keyword{internal}
