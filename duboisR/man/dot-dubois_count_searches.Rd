% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.dubois_count_searches}
\alias{.dubois_count_searches}
\title{Count stops and searches per (race, group), dropping unresolved rows}
\usage{
.dubois_count_searches(data, race_col, group_col, search_col)
}
\arguments{
\item{data}{A data frame.}

\item{race_col, group_col, search_col}{Column names.}
}
\value{
A data frame: \code{race}, \code{group}, \code{S} (rows where \code{search_col} is
\code{TRUE}), \code{n} (non-\code{NA} \code{search_col} rows).
}
\description{
The single aggregation step behind both \code{\link[=aggregate_sufficient_statistics]{aggregate_sufficient_statistics()}}
(the Threshold Test's input) and \code{\link[=summarize_county_search_rates]{summarize_county_search_rates()}} (the
Veil of Darkness search-rate chart) -- both ultimately answer "how many
stops, how many of them searched, per (race, group)" from the same
\code{search_col}. Kept as one shared helper instead of two independently
hand-rolled \code{aggregate()} calls that could silently disagree on how an
unresolved/\code{NA} \code{search_col} row is handled.
}
\keyword{internal}
