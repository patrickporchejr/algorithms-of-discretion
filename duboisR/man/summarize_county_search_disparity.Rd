% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/threshold_test.R
\name{summarize_county_search_disparity}
\alias{summarize_county_search_disparity}
\title{Aggregate county-level search-rate disparity, every race vs. a reference}
\usage{
summarize_county_search_disparity(
  county_search_rates,
  race_col = "subject_race",
  county_col = "county_fips",
  reference_race = "white"
)
}
\arguments{
\item{county_search_rates}{Output of \code{\link[=summarize_county_search_rates]{summarize_county_search_rates()}}.}

\item{race_col}{String race column. Default \code{"subject_race"}.}

\item{county_col}{String county column. Default \code{"county_fips"}.}

\item{reference_race}{Race every other race's search rate is compared
against. Default \code{"white"}.}
}
\value{
A tibble, one row per (county, non-reference race):
\verb{<county_col>}, \verb{<race_col>}, \code{n_searches}, \code{search_rate},
\code{n_searches_reference}, \code{search_rate_reference}, \code{disparity_ratio}
(\code{search_rate / search_rate_reference}).
}
\description{
Pivots \code{\link[=summarize_county_search_rates]{summarize_county_search_rates()}}'s long output against a single
reference race and computes each other race's search-rate ratio to it —
long format, one row per (county, non-reference race), so it covers
every race in the data at once instead of a hardcoded pair. This is a
pure frequency comparison (how often someone gets searched at all) —
see \code{\link[=aggregate_sufficient_statistics]{aggregate_sufficient_statistics()}}/\code{\link[=fit_threshold_test]{fit_threshold_test()}} for the
separate question of how \emph{justified} those searches are.
}
