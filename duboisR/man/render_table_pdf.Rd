% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{render_table_pdf}
\alias{render_table_pdf}
\title{Render a data frame as a paginated table PDF}
\usage{
render_table_pdf(
  df,
  path,
  title = NULL,
  wrap_cols = character(0),
  wrap_width = 40,
  rows_per_page = 12,
  base_size = 8,
  width = 11,
  height = 8.5
)
}
\arguments{
\item{df}{A data frame.}

\item{path}{Output PDF path.}

\item{title}{Optional title printed above the table on every page.}

\item{wrap_cols}{Character vector of column names to word-wrap before
rendering. Default none.}

\item{wrap_width}{Wrap width in characters for \code{wrap_cols}. Default 40.}

\item{rows_per_page}{Maximum rows per page. Default 12.}

\item{base_size}{Base font size (points) for table text. Default 8.}

\item{width, height}{PDF page size in inches. Default landscape letter
(11 x 8.5).}
}
\value{
\code{path}, invisibly.
}
\description{
A small \code{gridExtra}-based table renderer for exporting a data frame as a
standalone PDF -- for a table meant to go straight into a paper/report,
not just a Shiny page. Character columns named in \code{wrap_cols} are
word-wrapped to \code{wrap_width} first (a long free-text column -- a
rationale, a question prompt -- would otherwise force one illegibly wide
row); every other column is left as-is. Rows are split across pages,
\code{rows_per_page} each, so a many-row table doesn't get squeezed onto one
dense page.
}
