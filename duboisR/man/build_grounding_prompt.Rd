% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grounding_experiment.R
\name{build_grounding_prompt}
\alias{build_grounding_prompt}
\title{Build the system/user prompt for one naive-or-grounded LLM call}
\usage{
build_grounding_prompt(
  condition = c("naive", "grounded"),
  data_context,
  questions,
  datasheet = NULL
)
}
\arguments{
\item{condition}{\code{"naive"} or \code{"grounded"}.}

\item{data_context}{A string from \code{\link[=build_data_context]{build_data_context()}}.}

\item{questions}{A question list from \code{\link[=grounding_questions]{grounding_questions()}}.}

\item{datasheet}{A parsed datasheet list from \code{\link[=read_datasheet]{read_datasheet()}}.
Required when \code{condition = "grounded"}.}
}
\value{
A list with \code{system} and \code{user} prompt strings.
}
\description{
The "naive" condition gets \code{data_context} and the question battery only.
The "grounded" condition gets the same \code{data_context} and battery, plus
the full \code{datasheet.json} content and an explicit instruction to consult
it before answering — the single manipulated variable between the two
conditions.
}
