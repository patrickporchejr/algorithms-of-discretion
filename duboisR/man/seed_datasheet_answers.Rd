% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasheet.R
\name{seed_datasheet_answers}
\alias{seed_datasheet_answers}
\title{Seed or update a datasheet.json's answers non-interactively}
\usage{
seed_datasheet_answers(
  answers,
  path = "datasheet.json",
  overwrite_existing = FALSE
)
}
\arguments{
\item{answers}{A named list matching the structure of the package's
internal question schema: \code{list(section_key = list(question_key = "answer text", ...), ...)}. See a template from \code{\link[=use_datasheet]{use_datasheet()}} or
the section/question keys documented there for what's valid.}

\item{path}{Path to the \code{datasheet.json} to create or update. Default
\code{"datasheet.json"}.}

\item{overwrite_existing}{If \code{TRUE}, values in \code{answers} replace existing
non-empty answers for the same question. Default \code{FALSE} (fill blanks
only, preserving anything already answered).}
}
\value{
\code{path}, invisibly.
}
\description{
\code{build_datasheet_wizard()} is interactive-only by design (the reflection
is the point), but a first-pass draft still needs to come from
\emph{somewhere} -- e.g. seeding realistic demo content, or scripting an
update to one section after a diagnostic result changes. This is that
non-interactive path, with the same principle the wizard's own
\code{resume = TRUE} already applies: never silently destroy an existing
answer. By default, a question that already has a non-empty answer at
\code{path} is left untouched and only blank/missing questions are filled in
from \code{answers}; pass \code{overwrite_existing = TRUE} to intentionally
replace existing answers instead. Unrecognized section/question keys
(typos, or drift from the package's internal question schema) warn and
are skipped rather than silently written into the file.
}
