% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/threshold_test.R
\name{compare_outcome_threshold_test}
\alias{compare_outcome_threshold_test}
\title{Compare the naive outcome test to the Threshold Test's corrected estimate}
\usage{
compare_outcome_threshold_test(
  suff_stats,
  threshold_fit,
  reference_race = "white"
)
}
\arguments{
\item{suff_stats}{Output of \code{\link[=aggregate_sufficient_statistics]{aggregate_sufficient_statistics()}}.}

\item{threshold_fit}{Output of \code{\link[=fit_threshold_test]{fit_threshold_test()}}, fit on the same
\code{suff_stats} (or a superset covering the same races).}

\item{reference_race}{Race both gaps are computed against. Default
\code{"white"}.}
}
\value{
A tibble: \code{race}, \code{hit_rate}, \code{hit_rate_gap},
\code{mean_threshold_weighted}, \code{threshold_gap}, \code{agrees_in_direction}
(\code{TRUE} when \code{hit_rate_gap} and \code{threshold_gap} share a sign,
including both exactly \code{0}).
}
\description{
Puts the classic outcome test's hit-rate gap (\code{\link[=compute_outcome_test]{compute_outcome_test()}})
next to the Threshold Test's inferred-threshold gap
(\code{threshold_fit$summary}), both relative to the same \code{reference_race},
so the two can be read side by side. They live on different scales — an
observed conditional probability vs. an inferred risk cutoff — so this
isn't a before/after difference of one number; it's whether the two
methods agree on \emph{direction}. A negative gap means "held to a lower bar"
under either metric (lower hit rate among the searched, or a lower
inferred risk threshold for triggering a search), so matching signs mean
the two methods agree; a sign flip is the interesting case — it's the
signature of infra-marginality distorting the naive test (see Simoiu,
Corbett-Davies & Goel 2017, cited in \code{\link[=fit_threshold_test]{fit_threshold_test()}}).
}
