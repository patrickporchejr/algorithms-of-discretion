% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit_composition.R
\name{audit_composition}
\alias{audit_composition}
\title{Audit subpopulation composition and missingness}
\usage{
audit_composition(data, group_col, missing_col = NULL, weight_col = NULL)
}
\arguments{
\item{data}{A data frame.}

\item{group_col}{Character vector of one or more column names to group
by. When more than one is given, groups are intersectional composite
keys (e.g. \code{subject_race} x \code{subject_sex} -> \code{"black_female"}).}

\item{missing_col}{Optional string column name to compute
subgroup-specific missingness for (e.g. \code{"contraband_found"}).}

\item{weight_col}{Optional string column of row weights; if given,
counts become weighted sums instead of row counts. Not used by
default — forward-provisioned for survey-weighted data sources.}
}
\value{
A list of class \code{duboisR_composition} with elements
\code{representation} (tibble: \code{group}, \code{n}, \code{pct}) and \code{missingness}
(tibble: \code{group}, \code{n_total}, \code{n_missing}, \code{pct_missing}, or \code{NULL} if
\code{missing_col} was not supplied).
}
\description{
Computes the precise statistical ground truth needed to fill in a
datasheet's Composition section correctly (Gebru et al. 2021): total
instance counts and representation percentages per subpopulation, and
(optionally) subgroup-specific missingness rates, so a researcher can
check whether data is disproportionately missing or redacted for
marginalized groups. This function deliberately only computes and
formats these numbers — it does not narrate or interpret them. Filling
in the qualitative reflection is the researcher's job, not the
package's; see \code{\link[=use_datasheet]{use_datasheet()}}.
}
