% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subpop_disparities.R
\name{subpopulation_disparities}
\alias{subpopulation_disparities}
\title{Disaggregate model performance across intersectional subgroups}
\usage{
subpopulation_disparities(
  model_fit,
  data,
  actual_col,
  subgroup_cols = c("subject_race", "subject_sex"),
  threshold = 0.5,
  filter_search_only = NULL,
  search_col = "search_conducted"
)
}
\arguments{
\item{model_fit}{A \code{duboisR_glm_fit} (from \code{\link[=fit_audit_glm]{fit_audit_glm()}}) or a raw
\link[stats:glm]{stats::glm} object.}

\item{data}{A data frame to score (e.g. the fitting data, or a held-out
set).}

\item{actual_col}{String ground-truth outcome column (e.g.
\code{"search_conducted"} or \code{"contraband_found"}).}

\item{subgroup_cols}{Character vector of columns to form intersectional
subgroups from. Default \code{c("subject_race", "subject_sex")}.}

\item{threshold}{Probability threshold for a "predicted positive".
Default \code{0.5}.}

\item{filter_search_only}{If \code{NULL} (default), auto-set to \code{TRUE} when
\code{actual_col == "contraband_found"} (structurally \code{NA} off-search) and
\code{FALSE} otherwise. Can be forced explicitly.}

\item{search_col}{String search-indicator column, used only when
\code{filter_search_only} is \code{TRUE}. Default \code{"search_conducted"}.}
}
\value{
A tibble of class \code{duboisR_subpop_disparities} with columns
\code{group}, \code{n}, \code{base_rate}, \code{TPR}, \code{FPR}, \code{PPV}, \code{threshold_used}. Has
an attribute \code{"notes"} documenting the threshold, whether search-only
filtering was applied, and the fairness-metrics trade-off above.
}
\description{
A single accuracy or hit-rate number can hide starkly different error
rates for different subgroups. \code{subpopulation_disparities()} scores a
fitted model's predicted probabilities against a ground-truth outcome
and disaggregates True Positive Rate, False Positive Rate, and Positive
Predictive Value per intersectional subgroup (e.g. \code{"black_female"}).
}
\details{
It is a mathematical fact (Chouldechova 2017; Kleinberg, Mullainathan &
Raghavan 2016) that when base rates differ across groups, a calibrated
classifier (equal PPV across groups) generally \strong{cannot} also equalize
both FPR and FNR simultaneously. This function reports that trade-off —
it does not attempt to resolve it.
}
