% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/veil_of_darkness.R
\name{compute_daylight_status}
\alias{compute_daylight_status}
\title{Classify each stop as daylight, dark, or twilight}
\usage{
compute_daylight_status(
  data,
  date_col = "date",
  hour_col = "hour",
  county_fips_col = "county_fips",
  centroids = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{date_col}{String column name of the stop date. Default \code{"date"}.}

\item{hour_col}{String column name of the stop hour (integer 0-23).
Default \code{"hour"}.}

\item{county_fips_col}{String column name of the county FIPS code.
Default \code{"county_fips"}.}

\item{centroids}{Optional pre-loaded centroid tibble (see
\code{\link[=dubois_tx_centroids]{dubois_tx_centroids()}}); defaults to loading the bundled table.
Injectable for testing.}
}
\value{
\code{data} augmented with \code{stop_datetime}, \code{sunset}, \code{dusk},
\code{light_condition} (factor: \code{"daylight"}, \code{"dark"}, \code{"twilight"}), and
\code{is_dark} (logical; \code{NA} when \code{light_condition == "twilight"}).
}
\description{
Joins county centroids, computes each stop's sunset/civil-dusk time via
\code{\link[suncalc:getSunlightTimes]{suncalc::getSunlightTimes()}} (vectorized over unique \verb{(date, county)}
pairs, not per-row — required for acceptable performance at millions of
rows), and classifies each stop's lighting condition.
}
\details{
Because the data contract only carries an integer \code{hour} (no minutes;
see DESIGN.md \eqn{\S}4), each stop's timestamp is assumed to be at the
top of its recorded hour. This is a real, unavoidable resolution
limitation given the upstream pipeline, not a choice made for
convenience here.
}
