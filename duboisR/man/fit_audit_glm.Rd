% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glm_utils.R
\name{fit_audit_glm}
\alias{fit_audit_glm}
\title{Fit an audit GLM with closed-form Wald confidence intervals}
\usage{
fit_audit_glm(
  data,
  formula,
  family = "binomial",
  conf_level = 0.95,
  exponentiate = TRUE
)
}
\arguments{
\item{data}{A data frame.}

\item{formula}{A \link[stats:formula]{stats::formula} (e.g. from \code{\link[=build_formula]{build_formula()}}).}

\item{family}{Passed to \code{\link[stats:glm]{stats::glm()}}. Default \code{"binomial"}.}

\item{conf_level}{Confidence level for the Wald interval. Default \code{0.95}.}

\item{exponentiate}{If \code{TRUE} (default), exponentiate \code{estimate} and its
CI (e.g. to report odds ratios for a binomial/logit fit).}
}
\value{
A list of class \code{duboisR_glm_fit} with elements:
\describe{
\item{model}{The fitted \link[stats:glm]{stats::glm} object.}
\item{summary}{A tibble with columns \code{term}, \code{estimate}, \code{std.error},
\code{statistic}, \code{p.value}, \code{conf.low}, \code{conf.high}.}
}
}
\description{
Fits \code{glm(formula, data, family)}, then computes confidence intervals in
closed form from \code{summary(model)$coefficients} (\verb{estimate +/- z*SE} on
the link scale, then exponentiated if requested) rather than via
\code{broom::tidy(model, conf.int = TRUE)}. \code{broom} delegates to
\code{confint.glm()}, which defaults to profile-likelihood CIs — these require
iteratively refitting the model per coefficient, which is fine at
textbook sample sizes but effectively hangs at millions of rows. Wald CIs
are \code{O(1)} relative to the fit (no refitting) and are the standard choice
at this scale, since asymptotic normality of the MLE is a safe assumption
well before millions of rows.
}
