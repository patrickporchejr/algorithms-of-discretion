% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm_clients.R
\name{call_anthropic}
\alias{call_anthropic}
\title{Call Anthropic's Messages API for a forced structured-output answer}
\usage{
call_anthropic(
  system,
  user,
  schema,
  model,
  api_key = require_api_key("ANTHROPIC_API_KEY")
)
}
\arguments{
\item{system}{System prompt string.}

\item{user}{User prompt string (the data context, question battery, and
-- for the "grounded" condition -- the datasheet content).}

\item{schema}{A JSON-Schema list from \code{\link[=grounding_response_schema]{grounding_response_schema()}}.}

\item{model}{Model name, e.g. \code{"claude-opus-5"}.}

\item{api_key}{API key. Default reads \code{ANTHROPIC_API_KEY} from the
environment.}
}
\value{
A named list of \code{question_id -> answer} matching \code{schema}.
}
\description{
Forces a single tool call (\code{tool_choice}) against \code{schema} so the
response is always a strict, parseable answer object -- never free text
that would need prompt-specific parsing.
}
