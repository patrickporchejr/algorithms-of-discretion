% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grounding_experiment.R
\name{grounding_questions}
\alias{grounding_questions}
\title{The naive-vs-grounded question battery used by run_grounding_experiment()}
\usage{
grounding_questions()
}
\value{
The question battery, as a named list.
}
\description{
Returns the same fixed question list \code{data-raw/build_grounding_questions.R}
compiles into internal package data: each question has \code{type} (\code{"boolean"}
or \code{"enum"}), \code{prompt}, \code{choices} (enum only), \code{expected_answer}, and
\code{rationale} (which real, computed datasheet fact backs the expected
answer). Exposed so a consumer (e.g. the Shiny "LLM Grounding Test" tab)
can render question text/rationale without hardcoding a duplicate copy —
same pattern as \code{\link[=datasheet_schema]{datasheet_schema()}}.
}
