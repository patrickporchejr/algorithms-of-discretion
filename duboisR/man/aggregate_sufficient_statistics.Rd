% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/threshold_test.R
\name{aggregate_sufficient_statistics}
\alias{aggregate_sufficient_statistics}
\title{Aggregate stop-level data into sufficient statistics for the Threshold Test}
\usage{
aggregate_sufficient_statistics(
  data,
  race_col = "subject_race",
  group_col = "county_fips",
  search_col = "search_conducted",
  hit_col = "contraband_found",
  min_n = 20
)
}
\arguments{
\item{data}{A data frame.}

\item{race_col}{String race column. Default \code{"subject_race"}.}

\item{group_col}{String grouping column (a department/agency ID in the
original literature; county here). Default \code{"county_fips"}.}

\item{search_col}{String logical search column. Default
\code{"search_conducted"}.}

\item{hit_col}{String logical/\code{NA} hit column, \code{NA} when no search
occurred. Default \code{"contraband_found"}.}

\item{min_n}{Minimum stop count \code{n} for a cell to be retained. Default
\code{20}.}
}
\value{
A tibble of class \code{duboisR_suff_stats} with columns \code{race},
\verb{<group_col>}, \code{n}, \code{S} (searches), \code{H} (hits), \code{search_rate},
\code{hit_rate} (\code{NA} when \code{S == 0}).
}
\description{
Aggregates millions of stop-level rows into a small per-(race, group)
table of stop counts, search counts, and search "hit" counts — the
sufficient statistics the Threshold Test (see \code{\link[=fit_threshold_test]{fit_threshold_test()}})
needs. This dataset has no "department" field, so \code{group_col} defaults to
\code{county_fips}, the finest available administrative unit.
}
