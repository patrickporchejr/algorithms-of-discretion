% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/threshold_test.R
\name{restrict_to_top_counties}
\alias{restrict_to_top_counties}
\title{Restrict stop-level data to the highest-volume counties}
\usage{
restrict_to_top_counties(
  data,
  county_col = "county_fips",
  min_stops = 1000,
  top_n = 100
)
}
\arguments{
\item{data}{A data frame.}

\item{county_col}{String county column. Default \code{"county_fips"}.}

\item{min_stops}{Minimum total stops (any race) for a county to be
eligible. Default \code{1000}.}

\item{top_n}{Keep at most this many of the largest eligible counties.
Default \code{100}. Pass \code{Inf} to keep every county that clears \code{min_stops}.}
}
\value{
\code{data}, filtered to rows in the kept counties.
}
\description{
A coarser, pre-aggregation companion to \code{\link[=aggregate_sufficient_statistics]{aggregate_sufficient_statistics()}}'s
own per-(race, county) \code{min_n}: this filters at the whole-county level
(every race pooled together), before any race split, to counties with
enough total stops to be a priori stable, then keeps only the \code{top_n}
largest of those. Motivated by \code{\link[=fit_threshold_test]{fit_threshold_test()}} tending to land on
a near-degenerate (huge \code{a}, \code{b}) risk distribution when many small,
noisy counties dominate its per-cell squared-error objective — this is
one way to check whether that degeneracy is a data-sparsity artifact
rather than a real near-point-mass risk distribution.
}
