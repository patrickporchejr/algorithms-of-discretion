% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tendentious.R
\name{check_tendentious}
\alias{check_tendentious}
\title{Diagnose whether an outcome variable is "tendentious"}
\usage{
check_tendentious(
  variable_name,
  classification = NULL,
  rationale = NULL,
  interactive = rlang::is_interactive()
)
}
\arguments{
\item{variable_name}{String, the outcome variable being audited (e.g.
\code{"search_conducted"}).}

\item{classification}{One of \code{"objective"}, \code{"subjective"},
\code{"administrative"}. If \code{NULL} and \code{interactive} is \code{TRUE}, the user is
prompted to choose. If \code{NULL} and \code{interactive} is \code{FALSE}, this
errors — categorization must be explicit, never silently assumed.}

\item{rationale}{Optional free-text string explaining the
classification, for pasting into the datasheet's Uses section.}

\item{interactive}{Whether to prompt for \code{classification}/\code{rationale}
when not supplied. Default \code{\link[rlang:is_interactive]{rlang::is_interactive()}}.}
}
\value{
An object of class \code{duboisR_tendentious_check} with
\code{variable_name}, \code{classification}, \code{rationale}, \code{is_tendentious}, and
\code{message}.
}
\description{
Models trained on human decisions (arrest records, subjective evaluation
scores, an officer's decision to search) faithfully reproduce the biases
embedded in those decisions — they are not neutral measurements of an
underlying fact. \code{check_tendentious()} prompts the researcher to
explicitly categorize an outcome variable and raises a transparency
warning when it reflects administrative/subjective human discretion
rather than an objective measurement. This is a checklist tool, not an
automated judgment — the categorization decision is always the
researcher's.
}
