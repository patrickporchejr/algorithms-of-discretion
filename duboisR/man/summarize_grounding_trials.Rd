% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grounding_experiment.R
\name{summarize_grounding_trials}
\alias{summarize_grounding_trials}
\title{Collapse repeated-trial rows into one summary row per question/condition}
\usage{
summarize_grounding_trials(results)
}
\arguments{
\item{results}{The \code{results} tibble from a \code{duboisR_grounding_result}
(i.e. \code{run_grounding_experiment(...)$results}).}
}
\value{
A tibble, one row per (provider, model, condition, question_id):
\code{provider}, \code{model}, \code{condition}, \code{question_id}, \code{type}, \code{prompt},
\code{modal_answer}, \code{accuracy}, \code{agreement}, \code{mean_confidence}, \code{n_trials},
\code{expected_answer}, \code{rationale}.
}
\description{
\code{\link[=run_grounding_experiment]{run_grounding_experiment()}} with \code{n_repeats > 1} returns one row per
trial; this collapses those into, per (provider, model, condition,
question_id): the modal (most common) answer, the fraction of trials
that were correct, the fraction of trials that agreed with the modal
answer (a stability signal -- a single-run "changed answer" could be
sampling noise, a low-agreement one visibly is), and mean self-reported
confidence.
}
