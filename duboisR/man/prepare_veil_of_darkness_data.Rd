% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/veil_of_darkness.R
\name{prepare_veil_of_darkness_data}
\alias{prepare_veil_of_darkness_data}
\title{Prepare data for a Veil of Darkness test}
\usage{
prepare_veil_of_darkness_data(
  data,
  date_col = "date",
  hour_col = "hour",
  county_fips_col = "county_fips",
  race_col = "subject_race",
  race_ref = "white",
  centroids = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{date_col, hour_col, county_fips_col}{Passed to
\code{\link[=compute_daylight_status]{compute_daylight_status()}}.}

\item{race_col}{String race column. Default \code{"subject_race"}.}

\item{race_ref}{String reference level for \code{race_col}. Default \code{"white"}.}

\item{centroids}{Passed to \code{\link[=compute_daylight_status]{compute_daylight_status()}}.}
}
\value{
A list of class \code{duboisR_vod_prepared} with \code{fit_data} (the
intertwilight-restricted, race-releveled data ready to fit), plus the
diagnostic fields needed to build \code{\link[=fit_veil_of_darkness]{fit_veil_of_darkness()}}'s output.
}
\description{
Runs the expensive, outcome-independent half of \code{\link[=veil_of_darkness_test]{veil_of_darkness_test()}}:
daylight/dark classification (via \code{\link[=compute_daylight_status]{compute_daylight_status()}}), the
intertwilight-hour restriction, and the race releveling. None of this
depends on \code{outcome_var}, so callers that need to refit against several
outcomes (e.g. a Shiny reactive keyed on a UI dropdown) can compute this
once and reuse it across \code{\link[=fit_veil_of_darkness]{fit_veil_of_darkness()}} calls, instead of
repeating the ~2min daylight-classification pass on every refit.
}
