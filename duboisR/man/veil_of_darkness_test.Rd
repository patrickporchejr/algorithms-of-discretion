% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/veil_of_darkness.R
\name{veil_of_darkness_test}
\alias{veil_of_darkness_test}
\title{Fit a Veil of Darkness natural-experiment test}
\usage{
veil_of_darkness_test(
  data,
  outcome_var = "search_conducted",
  date_col = "date",
  hour_col = "hour",
  county_fips_col = "county_fips",
  race_col = "subject_race",
  race_ref = "white",
  interaction = FALSE,
  centroids = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{outcome_var}{String outcome column. Default \code{"search_conducted"}.}

\item{date_col, hour_col, county_fips_col}{Passed to
\code{\link[=compute_daylight_status]{compute_daylight_status()}}.}

\item{race_col}{String race column. Default \code{"subject_race"}.}

\item{race_ref}{String reference level for \code{race_col}. Default \code{"white"}.}

\item{interaction}{If \code{TRUE}, include a \code{race:is_dark} interaction term.
Default \code{FALSE}.}

\item{centroids}{Passed to \code{\link[=compute_daylight_status]{compute_daylight_status()}}.}
}
\value{
A list of class \code{duboisR_vod_result}; see \code{\link[=fit_veil_of_darkness]{fit_veil_of_darkness()}}.
}
\description{
The Veil of Darkness test evaluates racial profiling in traffic stops by
comparing the race distribution of stops made in daylight vs. after dark
— under the hypothesis that officers find it harder to observe a
driver's race at night (Grogger & Ridgeway 2006). This function
restricts the comparison to the \strong{intertwilight period}: clock hours
that are sometimes daylight and sometimes dark across the data's
date/county range, controlling for the confound that commuting patterns
(and thus who is on the road) vary by clock time regardless of race.
}
\details{
A convenience wrapper around \code{\link[=prepare_veil_of_darkness_data]{prepare_veil_of_darkness_data()}} +
\code{\link[=fit_veil_of_darkness]{fit_veil_of_darkness()}} for one-shot use. Callers that need to refit
against multiple outcomes against the same data should call those two
functions directly and reuse the prepared result -- see
\code{\link[=prepare_veil_of_darkness_data]{prepare_veil_of_darkness_data()}}.
}
