% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasheet.R
\name{use_datasheet}
\alias{use_datasheet}
\title{Scaffold a Datasheets-for-Datasets document}
\usage{
use_datasheet(
  path = "datasheet.md",
  format = c("md", "qmd"),
  overwrite = FALSE,
  open = rlang::is_interactive()
)
}
\arguments{
\item{path}{Where to write the datasheet. Default \code{"datasheet.md"}.}

\item{format}{\code{"md"} (default) or \code{"qmd"}.}

\item{overwrite}{Whether to overwrite an existing file at \code{path}.
Default \code{FALSE}.}

\item{open}{Whether to open the file in the default editor after
writing it. Default \code{\link[rlang:is_interactive]{rlang::is_interactive()}}.}
}
\value{
\code{path}, invisibly.
}
\description{
Copies a standardized, pre-formatted datasheet template — the seven
lifecycle sections of Gebru et al. 2021 ("Datasheets for Datasets"),
each with its standard sub-questions — into the researcher's project.
This deliberately copies \strong{static template text only}: the original
Datasheets for Datasets paper explicitly warns against automating the
qualitative reflection, since that reflection is the point. Use
\code{\link[=audit_composition]{audit_composition()}}, \code{\link[=check_proxies]{check_proxies()}}, and \code{\link[=check_tendentious]{check_tendentious()}} to
get the precise statistics some of the template's questions reference.
}
