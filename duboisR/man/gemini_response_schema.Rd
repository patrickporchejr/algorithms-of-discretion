% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm_clients.R
\name{gemini_response_schema}
\alias{gemini_response_schema}
\title{Convert a grounding_response_schema() into Gemini's OpenAPI-subset shape}
\usage{
gemini_response_schema(schema)
}
\arguments{
\item{schema}{A JSON-Schema list from \code{\link[=grounding_response_schema]{grounding_response_schema()}}.}
}
\value{
A schema list shaped for Gemini's \code{generationConfig.responseSchema}.
}
\description{
Gemini's \code{responseSchema} uses an OpenAPI-3.0 subset: uppercase type
names (\code{"OBJECT"}/\code{"STRING"}/\code{"NUMBER"}/\code{"INTEGER"}, not their lowercase
JSON-Schema equivalents), no \code{additionalProperties} keyword, and (per
observed 400s on unsupported keywords) no \code{minimum}/\code{maximum} bounds --
the 0-100 confidence range is enforced by prompt instructions instead.
\code{\link[=grounding_response_schema]{grounding_response_schema()}}'s output is always exactly two levels
(an object of per-question \verb{\{answer, confidence\}} objects), so this is a
direct, non-recursive translation rather than a general JSON-Schema
converter.
}
\keyword{internal}
