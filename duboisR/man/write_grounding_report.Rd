% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grounding_experiment.R
\name{write_grounding_report}
\alias{write_grounding_report}
\title{Write the naive-vs-grounded PDF report (table, chart, per-question table)}
\usage{
write_grounding_report(result, out_dir = "results", prefix = "grounding")
}
\arguments{
\item{result}{A \code{duboisR_grounding_result}.}

\item{out_dir}{Directory to write the three PDFs into. Created if it
doesn't exist. Default \code{"results"}.}

\item{prefix}{Filename prefix. Default \code{"grounding"}.}
}
\value{
Character vector of the three paths written, invisibly.
}
\description{
Three standalone PDFs for pulling straight into a paper/report -- the
same "Accuracy by provider/condition" table \code{\link[=format.duboisR_grounding_result]{format.duboisR_grounding_result()}}
prints, \code{\link[=plot_grounding_accuracy]{plot_grounding_accuracy()}}'s chart, and
\code{\link[=build_grounding_question_table]{build_grounding_question_table()}}'s condensed per-question table
(paginated, since it typically runs 50+ rows) -- all built from one
\code{\link[=summarize_grounding_trials]{summarize_grounding_trials()}} pass so the PDFs can't drift from what
the console/Shiny views show. Makes no API calls itself; call it on an
already-run \code{duboisR_grounding_result} (e.g. one just returned by
\code{\link[=run_grounding_experiment]{run_grounding_experiment()}}, or \code{readRDS()}ed back from a previous run)
to regenerate the PDFs for free.
}
