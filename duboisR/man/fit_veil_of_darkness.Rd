% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/veil_of_darkness.R
\name{fit_veil_of_darkness}
\alias{fit_veil_of_darkness}
\title{Fit a Veil of Darkness test against prepared data}
\usage{
fit_veil_of_darkness(
  prepared,
  outcome_var = "search_conducted",
  interaction = FALSE,
  control_map = list(),
  controls_selected = character(0)
)
}
\arguments{
\item{prepared}{A \code{duboisR_vod_prepared}, from
\code{\link[=prepare_veil_of_darkness_data]{prepare_veil_of_darkness_data()}}.}

\item{outcome_var}{String outcome column. Default \code{"search_conducted"}.}

\item{interaction}{If \code{TRUE}, include a \code{race:is_dark} interaction term.
Default \code{FALSE}.}

\item{control_map, controls_selected}{Passed to \code{\link[=build_formula]{build_formula()}} to layer
additional covariates (e.g. \code{subject_sex}, \code{poverty_rate}) onto the
race/is_dark/hour terms this function always includes -- the same
robustness-check pattern as the plain regression tab. Don't pass an
\code{hour}/\code{factor(hour)} term here: it's already included whenever the
intertwilight restriction leaves more than one distinct hour (see
below), so adding it again would duplicate the term.}
}
\value{
A list of class \code{duboisR_vod_result} with \code{model_fit} (a
\code{duboisR_glm_fit}), \code{diagnostics} (row counts / hours used), and
\code{caveats} (character vector, always including the nonreporting-
robustness assumption and the hour-only time-resolution limitation).
}
\description{
The cheap, outcome-dependent half of \code{\link[=veil_of_darkness_test]{veil_of_darkness_test()}}: builds
the formula and calls \code{\link[=fit_audit_glm]{fit_audit_glm()}} against \code{prepared$fit_data}. See
\code{\link[=prepare_veil_of_darkness_data]{prepare_veil_of_darkness_data()}}.
}
